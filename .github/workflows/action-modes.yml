name: Action modes

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: read

jobs:
  prebuilt:
    name: mode=prebuilt (always-on self-check)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-dotnet@v6
        with: { dotnet-version: "10.0.x" }
      - name: Build kql-guard (NativeAOT)
        run: dotnet publish -c Release -r linux-x64
      - id: scan
        uses: ./
        with:
          mode: prebuilt
          path: samples
      - name: Assert SARIF + exit-code
        run: |
          test -f "${{ steps.scan.outputs.sarif-file }}"
          echo "exit-code=${{ steps.scan.outputs.exit-code }}"
          [[ "${{ steps.scan.outputs.exit-code }}" =~ ^[012]$ ]]

  download:
    name: mode=download (needs a published release)
    runs-on: ubuntu-latest
    # ponytail: non-blocking while the repo is private — anonymous asset download
    # 404s. Goes green automatically once the repo is public (Marketplace state).
    continue-on-error: true
    steps:
      - uses: actions/checkout@v7
      - id: scan
        uses: ./
        with:
          mode: download
          path: samples
      - name: Assert SARIF + exit-code
        run: |
          test -f "${{ steps.scan.outputs.sarif-file }}"
          [[ "${{ steps.scan.outputs.exit-code }}" =~ ^[012]$ ]]

  docker:
    name: mode=docker (needs a published image)
    runs-on: ubuntu-latest
    # ponytail: non-blocking while the ghcr image is private. Goes green once public.
    continue-on-error: true
    steps:
      - uses: actions/checkout@v7
      - id: scan
        uses: ./
        with:
          mode: docker
          path: samples
      - name: Assert SARIF + exit-code
        run: |
          test -f "${{ steps.scan.outputs.sarif-file }}"
          [[ "${{ steps.scan.outputs.exit-code }}" =~ ^[012]$ ]]
